import { NextConfig } from "next";
const config: NextConfig = {
  reactStrictMode: true,
  images: {
    unoptimized: true, // Disables all image optimizations
  },
};



  
  export default config;
  